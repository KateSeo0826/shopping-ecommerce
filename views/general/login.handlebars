    <div class="container">
        <div class="login-container">`
        <form method="POST" action="/login" enctype="multipart/form-data">
            <p> <i class="fa fa-sign-out"></i>LOGIN</p>
            <input type="text" id="email" name="email" placeholder="E-mail" value="{{l_email}}">
            <div>{{#each error1}}{{this}}{{/each}} </div>
            <div>{{#each errors}}{{this}}{{/each}} </div><br>
            <input type="password" align="center" id="password" name="password" placeholder="Password">
            <div>{{#each error2}}{{this}}{{/each}} </div><br>
            <div>{{#each error4}}{{this}}{{/each}} </div><br>
            <button type="submit" value="login">LOGIN</button>
            <p class="forgot"><a href="#">Forgot Password?</p>
            </form>
    </div>
</div>